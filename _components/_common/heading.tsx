import { styled } from '~/stitches.config';
import { COLORS } from './_colors.css';

export const Heading = styled('h2', {
  lineHeight: '1.25',
  color: COLORS.TEXT,
  fontSize: '1.75rem',
  fontWeight: '600',
  marginBottom: '1.5rem',
});
